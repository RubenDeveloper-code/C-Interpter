#define _SUM_(x, y) ((x) + (y))
#define _MAX_(alo, bes) ((alo > bes) ? alo : bes)
#define ARGS int argc, char *argv[]
#define NONE
#define _TEXT_ "hola, buenos -    dias como + andamios"
#define _POW_(maincra) ((maincra) * (maincra))
int a();
