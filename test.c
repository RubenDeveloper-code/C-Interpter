#define sum(x, y, z) ((x) + (y) + (z))
#define gratVal (1 > 2 ? 3 : 4)
#define compare(x, y) ((x) > (y) ? x : y z)
int main() {
      funcionDeltest2();
      foo2(sum(5, 10, 15));
      foo3(sum(2, 7, 9));
      int a = gratVal;
      int z = gratVal;
      int b = compare(10, 2);
}
